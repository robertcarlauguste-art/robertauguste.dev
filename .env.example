# Absolute origin used to resolve metadata (Open Graph / Twitter image URLs).
# Set this in the deployment environment (Netlify: Site settings > Environment variables).
# Example: https://robertauguste.dev
SITE_URL=
